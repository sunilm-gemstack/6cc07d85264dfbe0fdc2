import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    flexRow: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: '10%',
    },
    container: {
        marginTop: '50%',
        margin: 20,
    },
    text: {
        fontSize: 18,
        fontWeight: 'bold',
        marginRight: '2%',
    },
    text2: {
        fontSize: 16,
        overflow: 'hidden',
        width: '60%',
    },
});

export default styles;
